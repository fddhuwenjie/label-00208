# Web Framework
streamlit==1.31.0

# Document Processing
python-docx==1.1.0
openpyxl==3.1.2
xlrd==2.0.1  # 旧版 .xls 格式支持
pandas==2.2.0
pdfplumber==0.10.4

# Chinese Text Processing
jieba==0.42.1

# Search Engine
Whoosh==2.7.4

# Semantic Analysis - Deep Learning (sentence-transformers)
# 注意：首次启动会下载模型（约 500MB），需要稳定网络连接
# 使用 CPU 版本的 PyTorch，避免下载无用的 CUDA 依赖，同时支持 Apple Silicon
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
sentence-transformers==2.5.1
transformers>=4.36.0

# Legacy (保留 gensim 用于兼容)
gensim==4.3.2
numpy==1.26.4
scipy==1.12.0

# Database
# SQLite is built-in

# Utilities
python-dateutil==2.8.2
chardet==5.2.0

# Testing
pytest==8.0.0
pytest-cov==4.1.0
